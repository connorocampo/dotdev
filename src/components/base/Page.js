import React from "react"

export default ({ children }) => (
  <div
    style={{
      height: "100%",
      margin: "0 auto",
      padding: "0 0 100px 0",
      maxWidth: "1200px",
    }}
  >
    {children}
  </div>
)
